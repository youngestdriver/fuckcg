# Example keystore.properties (do not commit real values)
# Copy to keystore.properties and fill with your own signing credentials.
storeFile=keystore/release.jks
storePassword=CHANGE_ME
keyAlias=release
keyPassword=CHANGE_ME

